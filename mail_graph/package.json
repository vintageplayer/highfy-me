{
  "name": "web3_mail",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ lazycoder1/dev_email",
    "create-local": "graph create --node http://localhost:8020/ lazycoder1/dev_email",
    "remove-local": "graph remove --node http://localhost:8020/ lazycoder1/dev_email",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lazycoder1/dev_email"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
